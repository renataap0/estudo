//Crie um programa que receba uma lista de nomes e retorne a lista ordenada em ordem alfabética.

function ordenarNomes(nomes) {
    return nomes.slice().sort();
}

console.log (ordenarNomes(['julia, joao, murilo, isaque, rafaela'])); 